<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/launchesFragment">
    <fragment
        android:id="@+id/launchesFragment"
        android:name="team.lf.spacex.ui.launches.LaunchesFragment"
        android:label="LaunchesFragment"
        tools:layout="@layout/fragment_all_launches"
        >
        <action
            android:id="@+id/action_launchesFragment_to_launchDetailFragment"
            app:destination="@id/launchDetailFragment"
            app:enterAnim="@anim/slide_in_right"
            app:exitAnim="@anim/slide_out_left"
            app:popEnterAnim="@anim/slide_in_left"
            app:popExitAnim="@anim/slide_out_right" />
    </fragment>
    <fragment
        android:id="@+id/launchDetailFragment"
        android:name="team.lf.spacex.ui.launch_detail.LaunchDetailFragment"
        android:label="LaunchDetailFragment"
        tools:layout="@layout/fragment_launch_detail"

        >
        <argument
            android:name="launch"
            app:argType="team.lf.spacex.domain.Launch" />
    </fragment>
</navigation>